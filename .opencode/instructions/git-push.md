# Git Push Rule

**NEVER** commit, push, or create PRs unless the user explicitly asks you to.

The user will say "git push" or "push" or "commit and push" when they want it. Do not push even after seemingly successful changes. Wait for the explicit command.
